# ============ 生产环境密钥配置 ============
# 复制为 .env 后填写真实值, 切勿提交仓库
# cp deploy/.env.example deploy/.env

# MySQL root 密码(必填, 自行设置强密码)
MYSQL_ROOT_PASSWORD=change-me-strong-password

# 邮箱 SMTP 账号与授权码(必填, 用于发送邮箱验证码)
MAIL_USERNAME=your-email@qq.com
MAIL_PASSWORD=your-smtp-auth-code

# JWT 签名密钥(必填, 至少32位随机字符串, 可用: openssl rand -base64 32)
JWT_SECRET=your-jwt-secret-at-least-32-chars

# 初始管理员密码(必填, 仅首次建库时用于创建 admin 账号, 建库后可任意改)
THESIS_ADMIN_PASSWORD=change-me-admin-password

# 前端访问地址(必填, 影响CORS, 按实际域名/IP填写, 例如: http://123.56.158.11)
# CORS_ORIGINS=http://123.56.158.11
